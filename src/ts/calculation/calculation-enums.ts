export enum CalculationRepaymentMode {
    CONSTANT_CAPITAL_PART,
    CONSTANT_INSTALLMENT,
    CAPITAL_AND_INTEREST_AT_END,
    CAPITAL_AT_END_INTEREST_PERIODICALLY
}

export enum CalculationRepaymentFrequency {
    MONTHLY, YEARLY
}

export enum CalculationPeriodInputType {
    MONTHS, YEARS
}
